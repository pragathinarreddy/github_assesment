import React from 'react'
import { gotData } from './gotData'

console.log(gotData)
function People() {
    // let house = gotData.houses.people?.map((each) => each.name)
    // let peopleName = house.people.map((each) => each.name)
    // console.log(peopleName,"peoplename")
  return (
    <div className='main'>

    </div>
  )
}

export default People